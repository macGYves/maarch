hostname: 'ThinkR'

system:
    processor: intel

timezone: Europe/Stockholm

partitions:
    esp: 'EFI_SYSTEM_PARTITION'
    encrypted: 'LUKS2_ENCRYPTED_PARTITION'

lvm:
    vg_main: 'vg.main'
    lv_root: 'root'
    lv_swap: 'swap'

user:
    name: macgyves
    group: users
    shell: /bin/zsh


base_packages:
    - base-devel
    - curl
    - git
    - openssh
    - rsync
    - zsh
