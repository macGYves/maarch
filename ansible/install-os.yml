---
- name: bootstrap Arch Linux system
  hosts: localhost
  remote_user: root
  vars_prompt:
    - name: luks_password
      prompt: "luks password"
      private: yes
      confirm: yes
    - name: install_disk
      prompt: 'Installation target device (e.g. /dev/sda)'
      private: no
    - name: root_password
      prompt: 'root password'
      private: yes
      encrypt: "sha512_crypt"
      confirm: yes
  roles:
    - os-install
  pre_tasks:
    - set_fact:
        root_password: "{{ root_password }}"
      tags: ['configure-base-os']


- name: configure bootstrapped Arch Linux installation
  hosts: localhost_chroot
  remote_user: root
  connection: chroot
  vars:
    ansible_chroot_exe: arch-chroot
  roles:
    - { role: localisation, tags: 'localisation' }
    - os-install--config
    - bootloader
  tags: ['configure-base-os']
